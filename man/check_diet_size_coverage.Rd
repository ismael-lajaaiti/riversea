% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_diet.R
\name{check_diet_size_coverage}
\alias{check_diet_size_coverage}
\title{Check that each species' diet is defined over a contiguous size range.}
\usage{
check_diet_size_coverage(diet)
}
\arguments{
\item{diet}{diet table with species, stage, length_min, length_max
columns.}
}
\value{
tibble with columns species, issue, detail. Empty if every
species' diet is defined over its whole size range.
}
\description{
Flags three failure modes: a gap between consecutive stages large enough
(greater than \code{diet_gap_tolerance}, to accommodate the small boundary
buffer literature sources use between stages) that a fish could fall
into it, a species whose first stage does not start at size 0, and a
species whose last stage does not reach an unbounded size.
\code{length_min}/\code{length_max} are assumed to be in centimeters. Run
\code{close_diet_size_gaps()} first to avoid flagging (and to stop
\code{build_metaweb()} erroring on) inter-stage buffers that are just
formatting.
}
