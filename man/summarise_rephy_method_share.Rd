% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rephy.R
\name{summarise_rephy_method_share}
\alias{summarise_rephy_method_share}
\title{Summarise REPHY measurement method share by year and parameter}
\usage{
summarise_rephy_method_share(raw_data)
}
\arguments{
\item{raw_data}{tibble as returned by \code{\link[=read_rephy]{read_rephy()}} (must still have the
method column, which \code{\link[=extract_nutrients_rephy]{extract_nutrients_rephy()}} drops).}
}
\value{
tibble with one row per (year, parameter_code, method), giving the
observation count and its share of that (year, parameter_code) total.
}
\description{
For each nutrient parameter and year, the share of observations recorded
with each measurement method. Expressing counts as a share (rather than
raw counts) makes method changes visible independently of the sampling
effort trend (see \code{\link[=plot_rephy_temporal_coverage]{plot_rephy_temporal_coverage()}}), which otherwise
dominates raw counts and masks method switches in low-effort years.
}
