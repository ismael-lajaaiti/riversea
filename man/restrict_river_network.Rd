% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amobio.R
\name{restrict_river_network}
\alias{restrict_river_network}
\title{Restrict the AMOBIO network to within \code{max_dist} of the kept river mouths}
\usage{
restrict_river_network(network, river_mouth, max_dist)
}
\arguments{
\item{network}{list(nodes, edges), see \code{\link[=extract_river_network]{extract_river_network()}}.}

\item{river_mouth}{tibble with \code{node_id}, \code{matched}, e.g. from
\code{\link[=match_mouth_district]{match_mouth_district()}}.}

\item{max_dist}{maximum channel-length distance to keep, in meters.}
}
\value{
list(nodes, edges) restricted to nodes within \code{max_dist}.
}
\description{
Grows the network outward (channel length, not straight-line) from every
matched river mouth at once, using a single Dijkstra run from a virtual
source connected to all of them with zero-weight edges - much cheaper than
one shortest-path search per mouth.
}
