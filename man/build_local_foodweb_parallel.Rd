% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_food_web.R
\name{build_local_foodweb_parallel}
\alias{build_local_foodweb_parallel}
\title{Build local food webs in parallel.}
\usage{
build_local_foodweb_parallel(
  ind_measure,
  local_id,
  metaweb,
  tab_size_classes,
  selected_resources,
  n_workers = parallel::detectCores()
)
}
\arguments{
\item{ind_measure}{output of \code{foodwebbuilder::remove_missing_species()}.}

\item{local_id}{column in \code{ind_measure} identifying the sampling operation.}

\item{metaweb}{metaweb matrix, as returned by \code{foodwebbuilder::build_metaweb()}.}

\item{tab_size_classes}{size class table, as returned by
\code{foodwebbuilder::compute_size_classes()}.}

\item{selected_resources}{which resources are considered at the base of
the web.}

\item{n_workers}{number of parallel workers, capped at the number of
distinct operations.}
}
\value{
named list of local food web matrices, one per operation id.
}
\description{
\code{foodwebbuilder::build_local_foodweb()} re-scans the entire \code{ind_measure}
table once per operation, so its cost grows with
\code{n_operations * n_individuals} rather than \code{n_individuals}. This splits
\code{ind_measure} into \code{n_workers} operation-disjoint chunks up front (so
each worker only ever scans its own small slice) and processes the
chunks in parallel forked processes, which share \code{metaweb} and
\code{tab_size_classes} via copy-on-write rather than each copying them —
memory use stays close to that of a single call, dominated by
\code{ind_measure} itself. Forking is Linux/macOS only; \code{mc.cores} silently
falls back to serial on Windows.
}
