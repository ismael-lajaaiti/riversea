% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_diet.R
\name{close_diet_size_gaps}
\alias{close_diet_size_gaps}
\title{Close small boundary buffers between consecutive diet stages.}
\usage{
close_diet_size_gaps(diet)
}
\arguments{
\item{diet}{diet table with species, stage, length_min, length_max
columns.}
}
\value{
diet table with small inter-stage gaps closed.
}
\description{
\code{foodwebbuilder::build_metaweb()} matches a fish's size to a diet
interval with \code{size >= length_min & size < length_max}. A tiny buffer
between stages (e.g. \code{length_max} of 3.0 followed by the next stage's
\code{length_min} of 3.1) is common in the source literature tables and looks
harmless, but a size-class midpoint that lands exactly in that 0.1 cm gap
matches neither interval and makes \code{build_metaweb()} error out. This
closes any gap up to \code{diet_gap_tolerance} by snapping a stage's
\code{length_max} to the next stage's \code{length_min}, so stages touch exactly
with no dead zone. Gaps larger than the tolerance are left untouched, so
they still surface as real coverage problems in
\code{check_diet_size_coverage()}.
}
