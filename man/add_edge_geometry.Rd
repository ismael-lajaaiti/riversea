% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amobio.R
\name{add_edge_geometry}
\alias{add_edge_geometry}
\title{Attach edge linestring geometry to an already-restricted network.}
\usage{
add_edge_geometry(network, paths)
}
\arguments{
\item{network}{list(nodes, edges) already restricted, e.g.
\code{restrict_river_network()}'s output.}

\item{paths}{character vector of AMOBIO file paths.}
}
\value{
\code{network} with \code{edges} gaining a \code{geometry} column
(linestring, same CRS as \code{nodes}).
}
\description{
\code{extract_river_network()} drops edge geometry to keep the
Dijkstra restriction cheap - \code{restrict_river_network()}'s node-to-node
distance only needs the precomputed \code{length}, not the shape of the
channel between them. Snapping to the nearest point on the true channel
(rather than the nearest node) needs that shape back, so this re-reads
just the geometry for the edges already kept by the restriction,
instead of redoing it with geometry attached throughout.
}
