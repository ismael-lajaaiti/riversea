% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_diet.R
\name{merge_diet}
\alias{merge_diet}
\title{Merge two diet tables.}
\usage{
merge_diet(diet, diet_new)
}
\arguments{
\item{diet}{diet table, as returned by \code{get_diet_validated()}.}

\item{diet_new}{diet table to merge in, taking precedence on any species
overlap.}
}
\value{
combined diet table, same schema as \code{diet}.
}
\description{
For a species present in both tables, \code{diet_new} wins entirely: all of
its rows are kept and \code{diet}'s rows for that species are dropped, rather
than mixing rows from both sources for the same species. Small
inter-stage boundary buffers are closed (see \code{close_diet_size_gaps()})
so no species has a size dead zone that would make \code{build_metaweb()}
error out.
}
