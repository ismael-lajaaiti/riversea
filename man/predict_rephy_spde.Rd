% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rephy_interpolation.R
\name{predict_rephy_spde}
\alias{predict_rephy_spde}
\title{Predict a REPHY parameter with an additive spatio-temporal SPDE model}
\usage{
predict_rephy_spde(train, newdata, mesh)
}
\arguments{
\item{train}{tibble with \code{value}, \code{longitude}, \code{latitude}, \code{year}, \code{month}.}

\item{newdata}{tibble with \code{longitude}, \code{latitude}, \code{year}, \code{month} to
predict at - years should stay within \code{train}'s range.}

\item{mesh}{\code{inla.mesh}, e.g. from \code{\link[=build_rephy_mesh]{build_rephy_mesh()}}.}
}
\value{
numeric vector, one prediction per row of \code{newdata}.
}
\description{
Fits \verb{log(value) ~ spatial Matern field (SPDE) + year (RW1) + cyclic month (RW1)}, additive only (no space-time interaction), and predicts at
\code{newdata}. Exact zeros (below detection limit) are set to half the
smallest positive value before fitting, instead of being dropped.
}
