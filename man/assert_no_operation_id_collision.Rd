% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_food_web.R
\name{assert_no_operation_id_collision}
\alias{assert_no_operation_id_collision}
\title{Assert that operation ids never collide between sea and river surveys.}
\usage{
assert_no_operation_id_collision(sea_operation, river_operation)
}
\arguments{
\item{sea_operation}{sea operation metadata, as in \code{sea_data_tidy$trait}.}

\item{river_operation}{river operation metadata, as returned by
\code{get_river_operation()}.}
}
\value{
TRUE, invisibly, if no operation id is shared between surveys.
}
\description{
\code{foodwebbuilder::build_local_foodweb()} groups individuals into local
food webs by \code{operation_id} alone, with no survey qualifier, so a
collision between a sea and a river id would silently pool two
unrelated samples into one fabricated local food web instead of
erroring. \code{merge_size()}/\code{merge_operation()} rely on ids staying unique
across surveys without prefixing them; run this first so a future data
refresh that introduces a collision fails loudly instead.
}
