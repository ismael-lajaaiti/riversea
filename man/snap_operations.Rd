% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amobio.R
\name{snap_operations}
\alias{snap_operations}
\title{Snap operations to the nearest AMOBIO river network edge.}
\usage{
snap_operations(operation, network)
}
\arguments{
\item{operation}{tibble with \code{operation_id}, \code{longitude}, \code{latitude}
(WGS84), \code{survey}, \code{district}, e.g. \code{classify_operation_location()}'s
output, restricted to inland operations.}

\item{network}{list(nodes, edges) with edge linestring geometry
(Lambert-93), e.g. \code{add_edge_geometry()}'s output.}
}
\value{
one row per distinct operation (\code{operation_id}, \code{longitude},
\code{latitude}, \code{survey}, \code{district}), with \code{node_id} (nearest network
node) and \code{snap_dist_m} (straight-line distance to the nearest edge,
in meters) added.
}
\description{
Matches against \code{restrict_river_network()}'s kept-catchment-only
output. \code{snap_dist_m} is the straight-line distance to the nearest point
on the nearest edge linestring, not to the nearest node - an operation
sitting midway along a straight stretch between two nodes isn't
penalized for it. \code{node_id} is still the nearest \emph{node} (needed
downstream for network routing), so it can be a step or two farther
than \code{snap_dist_m} alone would suggest.
}
