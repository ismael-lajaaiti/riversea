% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amobio.R
\name{summarise_env_outliers}
\alias{summarise_env_outliers}
\title{Summarise extreme values in environmental variables}
\usage{
summarise_env_outliers(
  data,
  vars = c("total_phosphorus", "phosphate", "ammonium", "nitrite", "nitrate",
    "kjeldahl_nitrogen"),
  log_transform = TRUE
)
}
\arguments{
\item{data}{tibble containing the environmental variables as columns.}

\item{vars}{character vector of variable names to check. Defaults to the
nutrient variables (phosphorus and nitrogen derivatives).}

\item{log_transform}{whether to compute the fences on the log10 scale.}
}
\value{
tibble with one row per variable.
}
\description{
Flags extreme values using Tukey's fences (values below
\code{Q1 - 1.5 * IQR} or above \code{Q3 + 1.5 * IQR} are flagged), computed on the
log10 scale by default since nutrient concentrations are typically
right-skewed. Non-positive values are excluded from the log-scale fences
(reported separately) since they cannot be log-transformed.
}
