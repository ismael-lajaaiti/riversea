% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amobio.R
\name{compute_sea_distance_to_mouth}
\alias{compute_sea_distance_to_mouth}
\title{Compute the coast-aware distance from each sea-side operation to the
nearest river mouth of its own district.}
\usage{
compute_sea_distance_to_mouth(operation, mouth, sea_graph, district_kept)
}
\arguments{
\item{operation}{tibble with \code{longitude}, \code{latitude}, \code{survey},
\code{inland}, \code{district}, e.g. \code{classify_operation_location()}'s output.}

\item{mouth}{sf tibble with \code{district}, \code{matched}, \code{geometry}
(WGS84), e.g. \code{match_mouth_district()}'s output.}

\item{sea_graph}{igraph graph with vertex attributes \code{x}/\code{y}, e.g.
\code{build_sea_graph()}'s output.}

\item{district_kept}{character vector of districts to compute for.}
}
\value{
sea-side rows of \code{operation}, restricted to \code{district_kept},
with \code{dist_mouth_m} added (meters).
}
\description{
Snaps each operation and matched mouth to the nearest reachable node
of \code{sea_graph} (see \code{snap_to_sea_graph()}), then runs a separate
multi-source Dijkstra per district (seeded from that district's own
matched mouths only) - see \code{compute_distance_to_mouth()} for the same
pattern on the river side.
}
