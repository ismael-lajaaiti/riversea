% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_food_web.R
\name{plot_local_foodweb}
\alias{plot_local_foodweb}
\title{Plot a single local food web as a trophic network, with species labels.}
\usage{
plot_local_foodweb(
  web,
  resource,
  diet,
  size,
  title = NULL,
  metric = c("mean", "max"),
  size_limits = NULL,
  label_size = 4
)
}
\arguments{
\item{web}{adjacency matrix, as \code{aggregate_species_web()} expects.}

\item{resource}{character vector of basal resource node names.}

\item{diet}{table of fish diet, passed to \code{classify_metaweb_species()}.}

\item{size}{individual fish size table, restricted to this operation's
own catch (e.g. \code{size_year_filtered |> filter(operation_id == id)}), so
node size reflects the fish actually caught there, not a global average.}

\item{title}{optional plot title, e.g. the operation's location.}

\item{metric}{"mean" or "max" length per species.}

\item{size_limits}{fixed \code{c(min, max)} for the size scale, so several
\code{plot_local_foodweb()} plots share one legend (e.g. via
\code{patchwork::plot_layout(guides = "collect")}) instead of each getting
its own. Default \code{NULL} auto-scales to this plot's own data.}

\item{label_size}{species label font size.}
}
\value{
ggplot.
}
\description{
Same layout, coloring and sizing as \code{plot_metaweb()}, but meant for a
single operation's local food web (tens of species rather than ~150),
small enough that species names can be shown directly with repelled
labels.
}
