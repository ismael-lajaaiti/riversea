% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_food_web.R
\name{measure_foodweb_structure}
\alias{measure_foodweb_structure}
\title{Compute local food web metrics and attach operation metadata, in parallel.}
\usage{
measure_foodweb_structure(
  web_list,
  operation,
  resource,
  n_workers = parallel::detectCores()
)
}
\arguments{
\item{web_list}{output of \code{build_foodweb()} with \code{local = TRUE}.}

\item{operation}{tibble with an \code{operation_id} column identifying each
sampling operation, plus whatever metadata to attach (e.g. year,
longitude, latitude).}

\item{resource}{resource species list.}

\item{n_workers}{number of parallel workers, capped at the number of
local food webs.}
}
\value{
tibble of local food webs with metrics and operation metadata.
}
\description{
Splits the local food webs into \code{n_workers} chunks and computes
\code{compute_foodweb_metrics()} for each chunk in a forked worker (see
\code{build_local_foodweb_parallel()} for the memory-sharing rationale).
\code{get_trophic_length()} dominates the per-web cost, so this is worth
parallelizing even though each web's computation itself is cheap.
}
