% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amobio.R
\name{extract_river_network}
\alias{extract_river_network}
\title{Load the AMOBIO river network topology, stripped down to the minimum}
\usage{
extract_river_network(paths)
}
\arguments{
\item{paths}{character vector of AMOBIO file paths.}
}
\value{
list(nodes, edges): \code{nodes} has \code{node_id}, \code{geometry},
\code{is_river_mouth}; \code{edges} has \code{from}, \code{to}, \code{length} (meters).
}
\description{
The raw network is ~1GB on disk / ~2.9GB in memory (1M nodes, 1.1M edges,
~80 columns each - mostly links to unrelated monitoring networks, plus
full edge geometries). Keeps only what a shortest-path distance
computation needs: node geometry and the river-mouth flag, and edge
topology with the precomputed length (dropping edge geometry, which is
most of the memory).
}
