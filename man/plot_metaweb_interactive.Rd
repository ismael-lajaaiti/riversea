% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_food_web.R
\name{plot_metaweb_interactive}
\alias{plot_metaweb_interactive}
\title{Interactive version of \code{plot_metaweb()}, with species names on hover.}
\usage{
plot_metaweb_interactive(web, resource, diet, size, metric = c("mean", "max"))
}
\arguments{
\item{web}{adjacency matrix, as \code{aggregate_species_web()} expects.}

\item{resource}{character vector of basal resource node names.}

\item{diet}{table of fish diet, passed to \code{classify_metaweb_species()}.}

\item{size}{individual fish size table with \code{species_valid}/\code{length}
columns, e.g. \code{size_year_filtered}.}

\item{metric}{"mean" or "max" length per species.}
}
\value{
a \code{plotly} htmlwidget.
}
\description{
Rebuilds the same Sugiyama layout as \code{plot_metaweb()} but as plain
\code{ggplot2} point/segment geoms (rather than \code{ggraph}'s specialised network
geoms), since those translate reliably through \code{plotly::ggplotly()}. Text
labels are mapped to \code{plotly}'s hover tooltip instead of being drawn on
the plot, so species names stay legible on zoom without cluttering the
static view.
}
