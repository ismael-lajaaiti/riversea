% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amobio.R
\name{dedup_amobio_metrics}
\alias{dedup_amobio_metrics}
\title{Deduplicate AMOBIO metrics on (sandre_code, date).}
\usage{
dedup_amobio_metrics(metrics)
}
\arguments{
\item{metrics}{tibble}
}
\value{
tibble with one row per (sandre_code, date)
}
\description{
Some nodes have several candidate physicochemical stations
(\code{METADATA_PC_CdStation}) for the same date, which is dropped upstream.
This leaves rows that share \verb{(sandre_code, date)} but disagree on the
\verb{pc_n_*} nutrient columns, so those are averaged across candidates while
every other column (identical across candidates) is kept as is.
}
