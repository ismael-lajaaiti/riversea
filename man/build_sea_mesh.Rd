% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amobio.R
\name{build_sea_mesh}
\alias{build_sea_mesh}
\title{Build a water-only triangular mesh over the sea, excluding land.}
\usage{
build_sea_mesh(operation, mouth, land, district_kept, buffer_deg)
}
\arguments{
\item{operation}{tibble with \code{longitude}, \code{latitude}, \code{survey},
\code{inland}, \code{district}, e.g. \code{classify_operation_location()}'s output.}

\item{mouth}{sf tibble with \code{district}, \code{matched}, \code{geometry}
(WGS84), e.g. \code{match_mouth_district()}'s output.}

\item{land}{sf polygons (WGS84), e.g. \code{land_polygon()}'s output.}

\item{district_kept}{character vector of districts to build the mesh
for.}

\item{buffer_deg}{degrees of padding around the sea-side operations
and matched mouths.}
}
\value{
list(mesh, in_water, land): \code{mesh} is an \code{fm_mesh_2d} object,
\code{in_water} a logical vector (one per mesh triangle), \code{land} the
(cropped, simplified) polygon used as the boundary.
}
\description{
Domain is a buffered bounding box around the sea-side operations and
matched river mouths, minus \code{land}. \code{fmesher::fm_mesh_2d()} auto-pads
the mesh beyond this boundary regardless of land - \code{in_water} flags
which triangles actually lie in water.
}
