% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_food_web.R
\name{summarise_resource_diet}
\alias{summarise_resource_diet}
\title{Share of basal-resource feeding links going to each resource category.}
\usage{
summarise_resource_diet(web, resource)
}
\arguments{
\item{web}{adjacency matrix of one local food web.}

\item{resource}{character vector of basal resource node names.}
}
\value{
named numeric vector, one share per resource in \code{resource}.
}
\description{
For one local food web, computes each resource's out-degree (number of
predators feeding on it) as a share of all resource-based links, giving
the resource composition of the community's diet. \code{NA} for every
resource not present in \code{web} (rather than 0), so it can't be confused
with a resource that was present but unused.
}
